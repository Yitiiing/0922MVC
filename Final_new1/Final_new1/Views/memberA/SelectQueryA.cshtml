@model IEnumerable<Final_new1.Models.TableMemberAs1101605>
@{
    ViewBag.Title = "SelectQueryA";
}

<h2>桃園台產ID查詢</h2>

<form action="@Url.Action("MySelQueryA")" method="post">
    ID:
    <select name="ID">
        @foreach (var m in Model)
        {
            <option value="@m.mId">@m.mId</option>
        }
    </select>

    <br />
    <input type="submit" value="查詢" class="btn btn-primary" />

</form>

