@model IEnumerable<Final_new1.Models.TableMemberCs1101605>
@{
    ViewBag.Title = "SelectQueryC";
}

<h2>台中太陽神ID查詢</h2>

<form action="@Url.Action("MySelQueryC")" method="post">
    ID:
    <select name="ID">
        @foreach (var m in Model)
        {
            <option value="@m.mId">@m.mId</option>
        }
    </select>

    <br />
    <input type="submit" value="查詢" class="btn btn-primary" />

</form>

